import React, { useRef } from 'react'; const SketchPad = () => { const canvasRef = useRef(null); return ( <div style={{ border: '1px solid #42b983', padding: '15px' }}> <canvas ref={canvasRef} width='800' height='400' style={{ backgroundColor: 'white' }} /> <button onClick={() => canvasRef.current.getContext('2d').clearRect(0, 0, 800, 400)}>Clear</button> <button onClick={() => navigator.clipboard.writeText(canvasRef.current.toDataURL())}>Save</button> </div> ); }; export default SketchPad;
